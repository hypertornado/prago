{{define "accordions"}} 
    <div class="accordions">
        {{range $item := .}}
            <div class="accordion">
                <div class="accordion_header" onclick="this.closest('.accordion').classList.toggle('accordion-visible')">
                    {{if $item.Icon}}<img src="/admin/api/icons?file={{$item.Icon}}" class="accordion_header_icon">{{end}}
                    {{$item.Name}}
                </div>
                <div class="accordion_content">{{$item.Text}}</div>
            </div>
        {{end}}
    </div>
{{end}}