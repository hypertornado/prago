{{define "admin_layout"}}
<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Admin</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="/admin/normalize.css">
    <link rel="stylesheet" href="/admin/admin.css">
  </head>
  <body class="admin">
    <div class="admin_header">
        <ul class="admin_header_list admin_header_list-right">
            <li><a href="/admin/logout">Odhlásit se</a></li>
        </ul>

        <h1>{{.admin_header.appName}}</h1>

        <ul class="admin_header_list">
            <li><a href="{{.admin_header.prefix}}">Home</a></li>
            {{range $item := .admin_header.menu}}
                <li><a href="{{$item.url}}">{{$item.name}}</a></li>
            {{end}}
        </ul>
    </div>

    <div class="admin_content">
    {{tmpl .admin_yield .}}

    </div>
    
  </body>
</html>

{{end}}